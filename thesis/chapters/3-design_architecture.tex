\chapter{Design Architecture}\label{chap:design_architecture}
AST Extensions: What new node types did you introduce to represent float literals, float primitives, and operators?
Pass Pipeline Changes: Walk the reader through the compilation pipeline. Which existing passes had to change to propagate float types, and what new passes did you have to insert to deal with float semantics?